name: xstate_dart
description: XState-JSON-compatible statechart interpreter in pure Dart. Hierarchical, parallel, timed (injected clock), eventless transitions.
version: 0.1.3
repository: https://github.com/tps-reports/xstate_dart
topics:
  - statechart
  - state-machine
  - xstate
  - fsm
environment:
  sdk: ^3.10.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
